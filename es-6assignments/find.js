//4. Use find() to get the first name that has more than 5 characters.l
let enames=["chandu","ramu","somu","cheta","gnana","balaji"]
let result=enames.find((enames=>enames.length>=5))
    console.log(result);